export type FORM_TYPE = {
    name: string,
    phoneNo: string,
    email: string,
    courseType: string,
    courseSchedule: string
}

export type FORM_OPTIONS_TYPE = {
    courseTypes:string[],
    courseSchedules:string[]
}